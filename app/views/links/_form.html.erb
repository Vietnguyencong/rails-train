<%= form_for([@link.report, @link]) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name_link %><br />
    <%= f.text_field :name_link %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.select :status, Link::STATUS_OPTIONS %> 
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
